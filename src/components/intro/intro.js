import React from 'react';
import Container from '../container/container';
import './intro.scss';

function Intro () {
	return (
		<section className='intro'>
			<Container>
				<h1 className='intro-title'>Explore movies & series</h1>
			</Container>
		</section>
	);
}

export default Intro;
